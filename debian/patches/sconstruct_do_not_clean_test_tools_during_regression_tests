--- a/SConstruct
+++ b/SConstruct
@@ -1657,7 +1657,6 @@ check = env.Alias('check', [
     time_regress,
     unpack_regress,
     json_regress,
-    testclean,
     ])
 
 env.Alias('testregress', check)
