--- a/SConstruct
+++ b/SConstruct
@@ -1746,7 +1746,6 @@ check = env.Alias('check', [
     time_regress,
     unpack_regress,
     json_regress,
-    testclean,
     ])
 
 env.Alias('testregress', check)
