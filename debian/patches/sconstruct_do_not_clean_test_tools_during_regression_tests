--- a/SConstruct
+++ b/SConstruct
@@ -1687,7 +1687,6 @@ check = env.Alias('check', [
     time_regress,
     unpack_regress,
     json_regress,
-    testclean,
     ])
 
 env.Alias('testregress', check)
