From 8a603056229ccc39e00a6979b15002e6cd82909e Mon Sep 17 00:00:00 2001
From: "Eric S. Raymond" <esr@thyrsus.com>
Date: Sun, 22 Feb 2015 12:19:57 -0500
Subject: [PATCH] Fix unit test for C++ binding.

---
 test_gpsmm.cpp | 8 +++++---
 1 file changed, 5 insertions(+), 3 deletions(-)

diff --git a/test_gpsmm.cpp b/test_gpsmm.cpp
index 63596a3..13930f7 100644
--- a/test_gpsmm.cpp
+++ b/test_gpsmm.cpp
@@ -74,9 +74,11 @@ static void libgps_dump_state(struct gps_data_t *collect)
 		      collect->satellites_visible);
 	for (i = 0; i < collect->satellites_visible; i++) {
 	    (void)fprintf(stdout, "    %2.2d: %2.2d %3.3d %3.0f %c\n",
-			  collect->PRN[i], collect->elevation[i],
-			  collect->azimuth[i], collect->ss[i],
-			  collect->used[i] ? 'Y' : 'N');
+			  collect->skyview[i].PRN, 
+			  collect->skyview[i].elevation,
+			  collect->skyview[i].azimuth, 
+			  collect->skyview[i].ss,
+			  collect->skyview[i].used ? 'Y' : 'N');
 	}
     }
     if (collect->set & DEVICE_SET)
-- 
2.1.4

